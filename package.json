{
  "name": "meuspedidos-avaliacao",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "chimp-watch": "chimp --ddp=http://localhost:3000 --watch --mocha --path=tests",
    "test": "chimp --mocha --path=tests --mochaSlow=0"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  },
  "devDependencies": {
    "chimp": "^0.41.2"
  }
}
